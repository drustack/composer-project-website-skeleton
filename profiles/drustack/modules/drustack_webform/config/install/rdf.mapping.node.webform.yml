bundle: webform
dependencies:
  config:
    - node.type.webform
  module:
    - node
fieldMappings:
  body:
    properties:
      - schema:text
  field_attachment:
    properties:
      - schema:associatedMedia
  field_tags:
    properties:
      - schema:about
  title:
    properties:
      - schema:name
  webform:
    properties:
      - schema:text
id: node.webform
langcode: en
status: true
targetEntityType: node
types:
  - schema:WebPage
