bundle: tags
dependencies:
  config:
    - taxonomy.vocabulary.tags
  module:
    - taxonomy
fieldMappings:
  description:
    properties:
      - schema:description
  name:
    properties:
      - schema:name
id: taxonomy_term.tags
langcode: en
status: true
targetEntityType: taxonomy_term
types:
  - schema:Thing
