{% if content %}
    <div {{ attributes }}>
        <div class="container-clearfix">
            <div {{ region_attributes.content.addClass('layout__region', 'layout__region--content', 'clearfix') }}>
                {{ content.content }}
            </div>
        </div>
    </div>
{% endif %}
